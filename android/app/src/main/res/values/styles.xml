<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Framework theme — no library dependency required.
         Compose replaces this at runtime via MaterialTheme {}. -->
    <style name="AppTheme" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@color/splashBg</item>
        <item name="android:navigationBarColor">@color/splashBg</item>
        <item name="android:windowBackground">@color/splashBg</item>
    </style>

    <!-- Splash screen (core-splashscreen dep already in build.gradle) -->
    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splashBg</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/AppTheme</item>
    </style>
</resources>
